<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/ll_gridItems"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="@dimen/gridview_items_padding">

    <ImageView
        android:id="@+id/itemImage"
        android:layout_width="match_parent"
        android:layout_height="@dimen/gridview_items_image_height"
        android:scaleType="centerInside" />

    <TextView
        android:id="@+id/itemTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:padding="@dimen/gridview_items_title_padding"
        android:textColor="@color/gridview_items_title_text_color"
        android:textSize="@dimen/gridview_items_title_text_size"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/itemSubTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:textColor="@color/gridview_items_title_text_color"
        android:textSize="@dimen/gridview_items_title_text_size" />
</LinearLayout>
