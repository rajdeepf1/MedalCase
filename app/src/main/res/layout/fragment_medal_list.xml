<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".ui.fragments.MedalListFragment">
    <data>

        <variable
            name="medalListViewModel"
            type="com.example.medalcase.ui.viewmodels.MedalListViewModel" />

    </data>

<RelativeLayout
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white"
    >


    <RelativeLayout
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="@dimen/toolbar_height"
        android:background="@color/toolbar_background_color"
        android:paddingHorizontal="@dimen/toolbar_horizontal_padding"
        >

        <ImageButton
            android:layout_width="@dimen/back_button_width"
            android:layout_height="@dimen/back_button_height"
            android:src="@drawable/baseline_arrow_back_ios_new_24"
            android:layout_centerVertical="true"
            android:background="@color/toolbar_background_color"
            />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/header_text"
            android:textColor="@color/toolbar_header_text_color"
            android:textSize="@dimen/header_text_size"
            android:layout_centerInParent="true"
            />

        <ImageView
            android:layout_width="@dimen/toolbar_vert_width"
            android:layout_height="@dimen/toolbar_vert_height"
            android:src="@drawable/baseline_more_vert_24"
            android:layout_centerVertical="true"
            android:layout_alignParentEnd="true"

            />

    </RelativeLayout>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerview"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/toolbar"
         />

</RelativeLayout>
</layout>
